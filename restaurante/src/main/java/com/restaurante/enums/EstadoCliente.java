package com.restaurante.enums;

/** Estados del cliente ocasional, evaluados automáticamente cada día */
public enum EstadoCliente {
    CLIENTE_NUEVO,
    POSIBLE_ACTIVO,
    POSIBLE_INACTIVO,
    ACTIVO,
    INACTIVO,
    RECUPERADO,
    ELIMINADO,
    BLOQUEADO
}
